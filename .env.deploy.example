# GitHub Registry Authentication
# Get token from: https://github.com/settings/tokens
# Required scopes: read:packages, write:packages, delete:packages
GITHUB_USERNAME=your_github_username
GITHUB_TOKEN=your_github_personal_access_token

# VPS Configuration
VPS_HOST=your_vps_ip_address
VPS_USER=your_vps_username
VPS_KEY=~/.ssh/id_rsa
COMPOSE_PATH=~/balungpisah-landing

# Note: For multiple environments, copy this file to:
# - .env.deploy.staging (for staging deployment)
# - .env.deploy.production (for production deployment)
# Then configure each with environment-specific values
